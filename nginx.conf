server {
    listen 80;
    server_name your-domain.com;  # 替换为你的域名
    root /path/to/ITS4Teacher/dist;  # 替换为你的dist目录的绝对路径
    index index.html;

    # 启用 gzip 压缩
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/xml text/javascript application/json application/javascript application/xml+rss application/rss+xml application/atom+xml image/svg+xml text/javascript application/x-javascript application/x-font-ttf application/vnd.ms-fontobject font/opentype;

    # 处理 Vue Router 的历史模式
    location / {
        try_files $uri $uri/ /index.html;
        
        # 设置正确的 MIME 类型
        location ~ \.js$ {
            add_header Content-Type application/javascript;
        }
        
        location ~ \.css$ {
            add_header Content-Type text/css;
        }
    }

    # 静态资源缓存
    location /assets {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # 防止访问隐藏文件
    location ~ /\. {
        deny all;
    }
}

# 如果需要支持 HTTPS，添加以下配置
server {
    listen 443 ssl http2;
    server_name your-domain.com;  # 替换为你的域名
    
    ssl_certificate /path/to/ssl/cert.pem;  # 替换为你的SSL证书路径
    ssl_certificate_key /path/to/ssl/key.pem;  # 替换为你的SSL密钥路径
    
    root /path/to/ITS4Teacher/dist;  # 替换为你的dist目录的绝对路径
    index index.html;

    # 启用 gzip 压缩
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/xml text/javascript application/json application/javascript application/xml+rss application/rss+xml application/atom+xml image/svg+xml text/javascript application/x-javascript application/x-font-ttf application/vnd.ms-fontobject font/opentype;

    # 处理 Vue Router 的历史模式
    location / {
        try_files $uri $uri/ /index.html;
        
        # 设置正确的 MIME 类型
        location ~ \.js$ {
            add_header Content-Type application/javascript;
        }
        
        location ~ \.css$ {
            add_header Content-Type text/css;
        }
    }

    # 静态资源缓存
    location /assets {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # 防止访问隐藏文件
    location ~ /\. {
        deny all;
    }
}